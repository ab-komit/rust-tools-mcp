[package]
name = "mcp-plugin-sdk"
version = "0.1.0"
edition = "2021"

[dependencies]
mcp-plugin-types = { path = "../mcp-plugin-types" }
mcp-plugin-sdk-macros = { path = "../mcp-plugin-sdk-macros" }
schemars = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
